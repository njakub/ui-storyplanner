import React from "react";
import Menu from "./Menu/Menu";
import "./Header.scss";

const Header = () => {
  return (
    <div className="Header">
      <Menu />
    </div>
  );
};

export default Header;
